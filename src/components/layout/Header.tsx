import { ToggleThemeBtn } from "../ToggleThemeBtn";

const Header = () => {
  return (
    <header>
      <div className="container">
        <ToggleThemeBtn />
      </div>
    </header>
  );
};

export default Header;
